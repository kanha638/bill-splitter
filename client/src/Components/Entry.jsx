import React from 'react'

const Entry = () => {
  return (
    <>
        <div className="entry-container">
            // on hover show a button on top right corner which allow user to edit that entry
            // amount paid by : will have list of users who paid the amount listed in that entry
            // amount was spent by : will have list of users who spent the amount listed in that entry
            // on bottom show two button one save other cancel(for cancel edit)
            // before saving we have to check that the entry is valid or not
        </div>
    </>
  )
}

export default Entry